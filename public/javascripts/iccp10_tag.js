var cesc = new myslideshow('myslideshow', {
    'autoplay':true,
    'hide_buttons':true,
    'interval':10000 + Math.floor(Math.random()*2000),
    'page':1,
    'per_page':30,
    'tags':'iccp10,iccp2010'
});